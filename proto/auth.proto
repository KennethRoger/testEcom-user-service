syntax = "proto3";

package auth;

service AuthService {
  rpc GenerateToken (UserData) returns (TokenObject) {}
}

message UserData {
  string _id = 1;
  string role = 2;
}

message TokenObject {
  bool success = 1;
  string token = 2;
}